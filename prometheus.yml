global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "owbank"
    metrics_path: "/api/metrics"
    static_configs:
      # Use host.docker.internal to reach the Next.js app running on the host
      - targets: ["host.docker.internal:3000"]
